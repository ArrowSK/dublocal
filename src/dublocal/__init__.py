"""DubLocal package metadata.

Importing :mod:`dublocal` must be side-effect free. Production composition belongs in
explicit services and the launcher, never in package import hooks.
"""

__version__ = "0.6.0b7"
